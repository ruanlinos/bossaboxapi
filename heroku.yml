setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: ./Dockerfile
  config:
    REQUIREMENTS_FILENAME: heroku.yml
release:
  image: web
  command:
    - ./release.sh
run:
  web: nodemon src/index.js
